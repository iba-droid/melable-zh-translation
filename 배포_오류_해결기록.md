# Streamlit Cloud 배포 오류 해결 기록

**발생일** 2026-09-09
**앱** https://melable-zh-translation-agqv6xposn6kpqxvajysnr.streamlit.app
**증상** 접속 시 `😦 Oh no. Error running app.` 만 뜨고 앱이 전혀 안 열림
**해결 커밋** `c370d80` (빈 `packages.txt` 삭제)

---

## 1. 무슨 일이 있었나 — 로그 원문

```
[02:26:34] 📦 Processing dependencies...
[02:26:34] 📦 Apt dependencies were installed from
           /mount/src/melable-zh-translation/packages.txt using apt-get.

Hit:1 http://deb.debian.org/debian-security bullseye-security InRelease
Hit:2 http://deb.debian.org/debian trixie InRelease
Get:3 http://deb.debian.org/debian trixie-updates InRelease [47.3 kB]
...
E: Release file for http://deb.debian.org/debian-security/dists/bullseye-security/InRelease
   is expired (invalid since 1d 5h 13min 30s).
   Updates for this repository will not be applied.

[02:26:35] ❗️ installer returned a non-zero exit code
[02:26:35] ❗️ Error during processing dependencies!
```

로그를 보면 **파이썬 코드는 실행조차 되지 않았습니다.** `pip install`도 시작하지 못했고,
`translator_app.py`는 열리지도 않았습니다. 의존성 설치 단계에서 죽었습니다.

---

## 2. 원인 — 3단계 연쇄

### ① `packages.txt`가 존재해서 apt 단계가 실행됨

Streamlit Cloud는 빌드할 때 저장소 최상단에 `packages.txt`가 **있으면**
"이 앱은 OS 레벨 패키지가 필요하구나" 라고 판단하고 `apt-get` 단계를 실행합니다.

문제는 우리 `packages.txt`가 **0바이트 빈 파일**이었다는 점입니다.

```
$ wc -c packages.txt
0 packages.txt
```

과거 파일 업로드 STT 기능에 `openai-whisper` + `ffmpeg`를 쓰던 시절의 잔재입니다.
`d9cc52a`("배포 안정화: whisper/torch 제거로 무료 티어 OOM 방지") 커밋에서 whisper를
걷어냈는데, `packages.txt` 파일 자체는 지우지 않고 내용만 비워둔 상태로 남아 있었습니다.

> **핵심 포인트**: Streamlit Cloud는 파일의 *내용*이 아니라 *존재 여부*로 apt 실행을 결정합니다.
> 빈 파일이어도 apt 단계는 그대로 돌아갑니다. 설치할 패키지가 0개일 뿐, 단계를 건너뛰지 않습니다.

### ② apt 저장소 목록에 만료된 항목이 남아 있었음

`apt-get`은 패키지를 설치하기 전에 등록된 모든 저장소의 목록을 갱신합니다.
Streamlit Cloud 빌드 이미지의 저장소 목록에는 두 종류가 섞여 있습니다:

| 저장소 | Debian 버전 | 로그 결과 |
|---|---|---|
| `debian trixie`, `trixie-updates`, `trixie-security` | 13 (현행) | 정상 |
| `debian-security bullseye-security` | **11 (구버전)** | **만료 (E:)** |

Debian 저장소의 `InRelease` 파일에는 **`Valid-Until`(유효기간)** 필드가 들어 있습니다.
저장소가 주기적으로 갱신되는 동안은 이 값도 계속 갱신되지만, 해당 버전의 지원이 끝나
갱신이 멈추면 마지막 `Valid-Until`이 지나가면서 파일이 만료 상태가 됩니다.

이건 apt의 보안 기능입니다. 오래된 패키지 목록을 계속 쓰면 이미 알려진 취약점이 있는
구버전 패키지를 최신인 줄 알고 설치하게 되므로, apt는 만료된 목록을 아예 거부합니다.

로그의 `invalid since 1d 5h 13min` — **하루 전에 막 만료됐습니다.**
어제까지는 멀쩡히 돌던 앱이 오늘 갑자기 죽은 이유가 이것입니다.
우리가 뭘 건드려서 깨진 게 아니라, **날짜가 지나서 깨졌습니다.**

### ③ apt가 실패하면 빌드 전체가 중단됨

만료된 저장소를 만난 `apt-get`은 `E:` 에러를 내고 **0이 아닌 종료 코드**를 반환합니다.
Streamlit Cloud는 이걸 "의존성 설치 실패"로 처리하고 그 자리에서 빌드를 끝냅니다.

```
❗️ installer returned a non-zero exit code
❗️ Error during processing dependencies!
```

그 뒤에 와야 할 `pip install`도, 앱 실행도 없습니다.
사용자 화면에는 이유가 안 나오고 `Oh no. Error running app.` 만 표시됩니다.

---

## 3. 한 줄 요약

> **설치할 패키지가 하나도 없는 빈 파일 하나 때문에, 쓰지도 않는 apt 단계가 돌았고,
> 그 apt가 참조하는 구버전 Debian 저장소가 어제 만료되면서 앱 전체가 죽었습니다.**

우리 코드에는 아무 문제가 없었습니다. 실제로 같은 커밋을 로컬(Python 3.13.5)에서
돌렸을 때 정상 기동(HTTP 200)을 확인했습니다.

---

## 4. 조치

`packages.txt`를 **삭제**했습니다.

```bash
git rm packages.txt
git commit -m "배포 복구: 빈 packages.txt 제거 (apt 단계 스킵)"
git push
```

파일이 없으면 Streamlit Cloud는 apt 단계를 **아예 건너뛰고** 바로 `pip install`로 넘어갑니다.
만료된 저장소를 만날 일 자체가 없어집니다.

**삭제해도 안전한 이유** — 이 앱은 OS 패키지가 전혀 필요 없습니다.
`requirements.txt`의 `streamlit / anthropic / pandas / requests / youtube-transcript-api`는
모두 순수 파이썬 패키지입니다. 유일하게 `ffmpeg`가 필요했던 whisper 코드는 이미
`except ImportError` 로 감싸져 있어, 클라우드에서는 "파일 STT는 로컬에서만 가능합니다"
안내를 띄우고 정상적으로 넘어갑니다.

---

## 5. 정상 복구 확인 방법

배포 로그에서 이 줄이 **사라졌으면** 정상입니다:

```
📦 Apt dependencies were installed from ... using apt-get.
```

apt 관련 로그 없이 곧바로 `pip install` 로그가 나오고 앱이 뜨면 성공입니다.

---

## 6. 재발 방지

- **`packages.txt`는 실제로 설치할 apt 패키지가 있을 때만 만들 것.**
  비워둘 거면 파일 자체를 삭제해야 합니다. 빈 파일로 남기면 이 사고가 그대로 재현됩니다.
- 같은 이유로 **의존성을 제거하는 커밋에서는 관련 설정 파일도 같이 지웠는지 확인**해야 합니다.
  이번 건은 `d9cc52a`에서 whisper를 지울 때 `packages.txt`를 비우기만 하고 남겨둔 게 두 달 뒤 터진 경우입니다.
- 앱이 갑자기 죽었는데 코드를 건드린 적이 없다면, **먼저 배포 로그부터 확인**하는 게 빠릅니다.
  (`share.streamlit.io` → 앱 → ⋮ → Logs). 이번에도 로그 한 번에 원인이 특정됐습니다.

---

## 참고 — 함께 처리한 작업 (커밋 `2eb647b`)

이 오류와 별개로 같은 세션에서 두 가지를 추가로 반영했습니다.

1. **저장 영구화** — Streamlit Cloud는 파일시스템이 휘발성이라 대시보드에서 저장한
   제품 데이터가 재시작 때마다 사라지고 있었습니다. `save_memory()`가 GitHub Contents API로
   `product_memory.json`을 저장소에 자동 커밋하도록 변경했습니다.
   **secrets에 `GITHUB_TOKEN`(Contents Read/Write 권한 PAT) 설정이 필요합니다.**
2. **엑셀 학습 데이터 반영** — `메라블_번역학습데이터.xlsx`의 번체 표현 231건·번역 규칙 178건을
   `data/learning_data.json`으로 변환(`build_learning_data.py`)해 제품·톤별로 프롬프트에 주입.
